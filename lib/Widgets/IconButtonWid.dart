import 'package:flutter/material.dart';

class IconButtonWid extends StatelessWidget {
  const IconButtonWid({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
        onPressed: (){},
        icon: Icon(Icons.ad_units)
    );
  }
}
