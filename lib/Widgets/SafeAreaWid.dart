import 'package:flutter/material.dart';

class SafeAreaWid extends StatelessWidget {
  const SafeAreaWid({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Container()
    );
  }
}
